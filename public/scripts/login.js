let loginForm = document.getElementById('login-form');

loginForm.addEventListener('submit', () => {
    let userEmail = loginForm.elements[0].value;
    let password = loginForm.elemetns[1].value;
})